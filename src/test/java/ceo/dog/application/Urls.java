package ceo.dog.application;

public class Urls {

    // This demo project is only ever executed against a live environment (as noted in the README).
    // For a project that executes against multiple environments (alpha/beta/gamma, dev/test/staging/production, etc.) a conditional statement would determine the base URL, e.g., using System.getProperty to read the pipeline stage in which the tests are being executed.

    public static final String DOG_API = "https://dog.ceo/api/";

}
